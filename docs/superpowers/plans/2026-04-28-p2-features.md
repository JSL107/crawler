# P2 Features Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v2 ideation doc 의 미구현 P2 항목 7개 (OPS-5, PRO-4, OPS-3, PM-3', PO-1, QA-1, OPS-2) 를 순서대로 구현한다.

**Architecture:** 각 항목은 독립 NestJS 모듈 또는 기존 모듈 확장. 공통 패턴은 DDD (domain/application/infrastructure), BullMQ repeatable job, Prisma db push, Bolt slash command. 외부 부작용이 있는 명령은 모두 PreviewGate 를 거친다.

**Tech Stack:** NestJS, BullMQ, Prisma (PostgreSQL), Slack Bolt, TypeScript, `pnpm test/lint:check/build`

---

## 구현 순서 근거

| 순서 | 항목 | 이유 |
|---|---|---|
| 1 | OPS-5 Failure Replay | 의존 없음, 가장 작은 범위 |
| 2 | PRO-4 Weekly Summarizer | MorningBriefing 패턴 그대로 복제 |
| 3 | OPS-3 Slack Reaction → Inbox | 새 DB 테이블 + Bolt 이벤트 + 컨텍스트 주입 |
| 4 | PM-3' FTS | GIN 인덱스 + queryRaw + 컨텍스트 주입 |
| 5 | PO-1 `/po-expand` | 새 에이전트 + 기존 PreviewGate 활용 |
| 6 | QA-1 Reviewer Learning | 새 DB 테이블 + 프롬프트 주입 |
| 7 | OPS-2 Webhook 수신부 | HMAC 가드 + HTTP 컨트롤러 |

---

## Task 1: OPS-5 Failure Replay

**Files:**
- Modify: `src/agent-run/domain/agent-run.type.ts`
- Modify: `src/agent-run/domain/port/agent-run.repository.port.ts`
- Modify: `src/agent-run/infrastructure/agent-run.prisma.repository.ts`
- Create: `src/agent-run/application/retry-run.usecase.ts`
- Create: `src/agent-run/application/retry-run.usecase.spec.ts`
- Modify: `src/agent-run/agent-run.module.ts`
- Modify: `src/slack/handler/agent-command.handler.ts`
- Modify: `src/slack/slack.service.ts`

- [ ] **Step 1: TriggerType 에 FAILURE_REPLAY 추가**

`src/agent-run/domain/agent-run.type.ts` 의 `TriggerType` enum 끝에 추가:

```typescript
  FAILURE_REPLAY = 'FAILURE_REPLAY',
```

- [ ] **Step 2: FailedRunSnapshot 인터페이스 + findById 메서드를 repository port 에 추가**

`src/agent-run/domain/port/agent-run.repository.port.ts` 에 추가:

```typescript
export interface FailedRunSnapshot {
  id: number;
  agentType: string;
  inputSnapshot: unknown;
  status: string;
}

// AgentRunRepositoryPort 인터페이스에 메서드 추가:
  findById(id: number): Promise<FailedRunSnapshot | null>;
```

- [ ] **Step 3: 테스트 작성 (실패 확인)**

`src/agent-run/application/retry-run.usecase.spec.ts` 생성:

```typescript
import { RetryRunUsecase } from './retry-run.usecase';
import { AGENT_RUN_REPOSITORY_PORT } from '../domain/port/agent-run.repository.port';
import { AgentRunStatus } from '../domain/agent-run.type';

describe('RetryRunUsecase', () => {
  const mockRepo = {
    findById: jest.fn(),
    begin: jest.fn(),
    finish: jest.fn(),
    recordEvidence: jest.fn(),
    findLatestSucceededRun: jest.fn(),
    findRecentSucceededRuns: jest.fn(),
    aggregateQuotaStats: jest.fn(),
  };

  const usecase = new RetryRunUsecase(mockRepo as any);

  beforeEach(() => jest.clearAllMocks());

  it('존재하지 않는 run ID 면 null 반환', async () => {
    mockRepo.findById.mockResolvedValue(null);
    const result = await usecase.execute({ id: 999 });
    expect(result).toBeNull();
  });

  it('FAILED 가 아닌 run 이면 null 반환', async () => {
    mockRepo.findById.mockResolvedValue({
      id: 1, agentType: 'PM', inputSnapshot: {}, status: AgentRunStatus.SUCCEEDED,
    });
    const result = await usecase.execute({ id: 1 });
    expect(result).toBeNull();
  });

  it('FAILED run 이면 snapshot 반환', async () => {
    mockRepo.findById.mockResolvedValue({
      id: 2, agentType: 'PM', inputSnapshot: { tasksText: 'foo', slackUserId: 'U123' }, status: AgentRunStatus.FAILED,
    });
    const result = await usecase.execute({ id: 2 });
    expect(result).toEqual({
      id: 2, agentType: 'PM', inputSnapshot: { tasksText: 'foo', slackUserId: 'U123' },
    });
  });
});
```

- [ ] **Step 4: 테스트 실행 확인 (실패)**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="retry-run.usecase.spec" 2>&1 | tail -20
```

Expected: `Cannot find module './retry-run.usecase'`

- [ ] **Step 5: RetryRunUsecase 구현**

`src/agent-run/application/retry-run.usecase.ts` 생성:

```typescript
import { Inject, Injectable } from '@nestjs/common';

import { AgentRunStatus } from '../domain/agent-run.type';
import {
  AGENT_RUN_REPOSITORY_PORT,
  AgentRunRepositoryPort,
  FailedRunSnapshot,
} from '../domain/port/agent-run.repository.port';

export interface RetryRunPayload {
  id: number;
  agentType: string;
  inputSnapshot: unknown;
}

@Injectable()
export class RetryRunUsecase {
  constructor(
    @Inject(AGENT_RUN_REPOSITORY_PORT)
    private readonly repository: AgentRunRepositoryPort,
  ) {}

  async execute({ id }: { id: number }): Promise<RetryRunPayload | null> {
    const run: FailedRunSnapshot | null = await this.repository.findById(id);
    if (!run || run.status !== AgentRunStatus.FAILED) {
      return null;
    }
    return { id: run.id, agentType: run.agentType, inputSnapshot: run.inputSnapshot };
  }
}
```

- [ ] **Step 6: AgentRunPrismaRepository 에 findById 구현**

`src/agent-run/infrastructure/agent-run.prisma.repository.ts` 에 추가:

```typescript
  async findById(id: number): Promise<FailedRunSnapshot | null> {
    const row = await this.prisma.agentRun.findUnique({
      where: { id },
      select: { id: true, agentType: true, inputSnapshot: true, status: true },
    });
    if (!row) return null;
    return {
      id: row.id,
      agentType: row.agentType,
      inputSnapshot: row.inputSnapshot as unknown,
      status: row.status,
    };
  }
```

import 에 `FailedRunSnapshot` 추가 필요.

- [ ] **Step 7: 테스트 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="retry-run.usecase.spec" 2>&1 | tail -10
```

Expected: `Tests: 3 passed`

- [ ] **Step 8: AgentRunModule 에 RetryRunUsecase export 추가**

`src/agent-run/agent-run.module.ts`:

```typescript
import { RetryRunUsecase } from './application/retry-run.usecase';

// providers 에 RetryRunUsecase 추가, exports 에도 추가:
providers: [AgentRunService, GetQuotaStatsUsecase, RetryRunUsecase, ...],
exports: [AgentRunService, GetQuotaStatsUsecase, RetryRunUsecase],
```

- [ ] **Step 9: /retry-run 핸들러 추가 (agent-command.handler.ts)**

`src/slack/handler/agent-command.handler.ts` 의 `deps` 타입과 함수 끝에 추가:

```typescript
// deps 인터페이스에 추가:
    retryRunUsecase: RetryRunUsecase;
    generateDailyPlanUsecase: GenerateDailyPlanUsecase;  // already exists
    // 이미 있는 나머지 usecases...

// registerAgentCommandHandlers 함수 끝에 추가:
  app.command('/retry-run', async ({ ack, command, respond }) => {
    const idStr = command.text?.trim() ?? '';
    const id = parseInt(idStr, 10);
    if (!idStr || isNaN(id)) {
      await ack({ response_type: 'ephemeral', text: '사용법: `/retry-run <AgentRun ID>` (예: `/retry-run 42`)' });
      return;
    }
    await ack({ response_type: 'ephemeral', text: `AgentRun #${id} 재실행 중입니다...` });

    const payload = await deps.retryRunUsecase.execute({ id });
    if (!payload) {
      await respond({ response_type: 'ephemeral', replace_original: true, text: `AgentRun #${id} 를 찾을 수 없거나 FAILED 상태가 아닙니다.` });
      return;
    }

    const snapshot = payload.inputSnapshot as Record<string, unknown>;
    const slackUserId = (snapshot.slackUserId as string | undefined) ?? command.user_id;

    try {
      switch (payload.agentType) {
        case 'PM':
          await runAgentCommand({
            respond, logger: deps.logger, commandLabel: `/retry-run#${id} (PM)`,
            execute: () => deps.generateDailyPlanUsecase.execute({
              tasksText: (snapshot.tasksText as string) ?? '',
              slackUserId,
              triggerType: TriggerType.FAILURE_REPLAY,
            }),
            format: (result) => formatDailyPlan(result.plan, result.sources),
          });
          break;
        case 'WORK_REVIEWER':
          await runAgentCommand({
            respond, logger: deps.logger, commandLabel: `/retry-run#${id} (WORK_REVIEWER)`,
            execute: () => deps.generateWorklogUsecase.execute({
              workText: (snapshot.workText as string) ?? '',
              slackUserId,
            }),
            format: formatDailyReview,
          });
          break;
        case 'CODE_REVIEWER':
          await runAgentCommand({
            respond, logger: deps.logger, commandLabel: `/retry-run#${id} (CODE_REVIEWER)`,
            execute: () => deps.reviewPullRequestUsecase.execute({
              prRef: (snapshot.prRef as string) ?? '',
              slackUserId,
            }),
            format: (review) => formatPullRequestReview({ prRef: (snapshot.prRef as string) ?? '', review }),
          });
          break;
        case 'IMPACT_REPORTER':
          await runAgentCommand({
            respond, logger: deps.logger, commandLabel: `/retry-run#${id} (IMPACT_REPORTER)`,
            execute: () => deps.generateImpactReportUsecase.execute({
              subject: (snapshot.subject as string) ?? '',
              slackUserId,
            }),
            format: formatImpactReport,
          });
          break;
        case 'PO_SHADOW':
          await runAgentCommand({
            respond, logger: deps.logger, commandLabel: `/retry-run#${id} (PO_SHADOW)`,
            execute: () => deps.generatePoShadowUsecase.execute({
              extraContext: (snapshot.extraContextLength ? '' : ''),
              slackUserId,
            }),
            format: formatPoShadowReport,
          });
          break;
        default:
          await respond({ response_type: 'ephemeral', replace_original: true, text: `AgentType '${payload.agentType}' 의 재실행은 지원되지 않습니다.` });
      }
    } catch (error: unknown) {
      deps.logger.error(`/retry-run#${id} 실패: ${error instanceof Error ? error.message : String(error)}`);
      await respond({ response_type: 'ephemeral', replace_original: true, text: `재실행 실패: ${error instanceof Error ? error.message : String(error)}` });
    }
  });
```

import 에 `TriggerType`, `formatDailyPlan`, `formatDailyReview`, `formatPullRequestReview`, `formatPoShadowReport`, `formatImpactReport`, `RetryRunUsecase` 추가.

- [ ] **Step 10: SlackService 에 RetryRunUsecase 주입**

`src/slack/slack.service.ts`:
- constructor 에 `private readonly retryRunUsecase: RetryRunUsecase` 추가
- `registerCommands` 의 `registerAgentCommandHandlers` 호출에 `retryRunUsecase: this.retryRunUsecase` 추가
- import 에 `RetryRunUsecase` 추가

- [ ] **Step 11: lint + test + build 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -30
```

Expected: 3중 green

---

## Task 2: PRO-4 Weekly Summarizer

**Files:**
- Create: `src/weekly-summary/domain/weekly-summary.type.ts`
- Create: `src/weekly-summary/application/weekly-summary.scheduler.ts`
- Create: `src/weekly-summary/application/weekly-summary.scheduler.spec.ts`
- Create: `src/weekly-summary/infrastructure/weekly-summary.consumer.ts`
- Create: `src/weekly-summary/weekly-summary.module.ts`
- Modify: `src/agent-run/domain/agent-run.type.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1: TriggerType 에 WEEKLY_SUMMARY_CRON 추가**

`src/agent-run/domain/agent-run.type.ts`:

```typescript
  WEEKLY_SUMMARY_CRON = 'WEEKLY_SUMMARY_CRON',
```

- [ ] **Step 2: weekly-summary 도메인 타입 정의**

`src/weekly-summary/domain/weekly-summary.type.ts` 생성:

```typescript
export const WEEKLY_SUMMARY_QUEUE = 'weekly-summary';

export interface WeeklySummaryJobData {
  ownerSlackUserId: string;
  target: string;
}

// 기본 cron — 매주 금요일 17:00 KST.
export const DEFAULT_WEEKLY_SUMMARY_CRON = '0 17 * * 5';
export const DEFAULT_WEEKLY_SUMMARY_TIMEZONE = 'Asia/Seoul';
```

- [ ] **Step 3: Scheduler 테스트 작성 (실패 확인)**

`src/weekly-summary/application/weekly-summary.scheduler.spec.ts` 생성:

```typescript
import { WeeklySummaryScheduler } from './weekly-summary.scheduler';

describe('WeeklySummaryScheduler', () => {
  const mockQueue = {
    getRepeatableJobs: jest.fn().mockResolvedValue([]),
    removeRepeatableByKey: jest.fn(),
    add: jest.fn(),
  };
  const mockConfig = { get: jest.fn() };

  it('WEEKLY_SUMMARY_OWNER_SLACK_USER_ID 미설정 시 graceful skip', async () => {
    mockConfig.get.mockReturnValue(undefined);
    const scheduler = new WeeklySummaryScheduler(mockQueue as any, mockConfig as any);
    await scheduler.onApplicationBootstrap();
    expect(mockQueue.add).not.toHaveBeenCalled();
  });

  it('owner 설정 시 BullMQ repeatable job 등록', async () => {
    mockConfig.get.mockImplementation((key: string) => {
      if (key === 'WEEKLY_SUMMARY_OWNER_SLACK_USER_ID') return 'U123';
      return undefined;
    });
    const scheduler = new WeeklySummaryScheduler(mockQueue as any, mockConfig as any);
    await scheduler.onApplicationBootstrap();
    expect(mockQueue.add).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 4: 테스트 실행 확인 (실패)**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="weekly-summary.scheduler.spec" 2>&1 | tail -10
```

Expected: `Cannot find module './weekly-summary.scheduler'`

- [ ] **Step 5: WeeklySummaryScheduler 구현**

`src/weekly-summary/application/weekly-summary.scheduler.ts` 생성:

```typescript
import { InjectQueue } from '@nestjs/bullmq';
import { Injectable, Logger, OnApplicationBootstrap } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Queue } from 'bullmq';

import {
  DEFAULT_WEEKLY_SUMMARY_CRON,
  DEFAULT_WEEKLY_SUMMARY_TIMEZONE,
  WEEKLY_SUMMARY_QUEUE,
  WeeklySummaryJobData,
} from '../domain/weekly-summary.type';

const SUMMARY_JOB_NAME = 'weekly-summary';

@Injectable()
export class WeeklySummaryScheduler implements OnApplicationBootstrap {
  private readonly logger = new Logger(WeeklySummaryScheduler.name);

  constructor(
    @InjectQueue(WEEKLY_SUMMARY_QUEUE) private readonly queue: Queue,
    private readonly configService: ConfigService,
  ) {}

  async onApplicationBootstrap(): Promise<void> {
    const owner = this.readOwnerOrNull();
    if (!owner) {
      this.logger.log('Weekly Summary 비활성 (WEEKLY_SUMMARY_OWNER_SLACK_USER_ID 미설정).');
      await this.cleanupExistingRepeatables();
      return;
    }

    const target = this.configService.get<string>('WEEKLY_SUMMARY_TARGET') ?? owner;
    const cron = this.readNonEmpty('WEEKLY_SUMMARY_CRON', DEFAULT_WEEKLY_SUMMARY_CRON);
    const tz = this.readNonEmpty('WEEKLY_SUMMARY_TIMEZONE', DEFAULT_WEEKLY_SUMMARY_TIMEZONE);

    await this.cleanupExistingRepeatables();

    const payload: WeeklySummaryJobData = { ownerSlackUserId: owner, target };
    await this.queue.add(SUMMARY_JOB_NAME, payload, {
      repeat: { pattern: cron, tz },
      jobId: `weekly-summary:${owner}->${target}`,
      removeOnComplete: 20,
      removeOnFail: 20,
      attempts: 2,
      backoff: { type: 'exponential', delay: 60_000 },
    });

    this.logger.log(`Weekly Summary 활성화 — owner=${owner}, target=${target}, cron="${cron}" (${tz})`);
  }

  private readOwnerOrNull(): string | null {
    const raw = this.configService.get<string>('WEEKLY_SUMMARY_OWNER_SLACK_USER_ID');
    if (!raw) return null;
    const trimmed = raw.trim();
    return trimmed.length > 0 ? trimmed : null;
  }

  private readNonEmpty(key: string, fallback: string): string {
    const raw = this.configService.get<string>(key);
    if (!raw) return fallback;
    const trimmed = raw.trim();
    return trimmed.length > 0 ? trimmed : fallback;
  }

  private async cleanupExistingRepeatables(): Promise<void> {
    const repeatables = await this.queue.getRepeatableJobs();
    for (const job of repeatables) {
      await this.queue.removeRepeatableByKey(job.key);
    }
  }
}
```

- [ ] **Step 6: 테스트 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="weekly-summary.scheduler.spec" 2>&1 | tail -10
```

Expected: `Tests: 2 passed`

- [ ] **Step 7: WeeklySummaryConsumer 구현**

`src/weekly-summary/infrastructure/weekly-summary.consumer.ts` 생성:

```typescript
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Inject, Logger } from '@nestjs/common';
import { Job } from 'bullmq';

import { AgentRunService } from '../../agent-run/application/agent-run.service';
import { TriggerType } from '../../agent-run/domain/agent-run.type';
import { AgentType } from '../../model-router/domain/model-router.type';
import { SLACK_NOTIFIER_PORT, SlackNotifierPort } from '../../morning-briefing/domain/port/slack-notifier.port';
import { formatDailyReview } from '../../slack/format/daily-review.formatter';
import { formatModelFooter } from '../../slack/format/model-footer.formatter';
import { GenerateWorklogUsecase } from '../../agent/work-reviewer/application/generate-worklog.usecase';
import { coerceToDailyPlan } from '../../agent/pm/domain/prompt/previous-plan-formatter';
import { WEEKLY_SUMMARY_QUEUE, WeeklySummaryJobData } from '../domain/weekly-summary.type';

// PRO-4 Weekly Summarizer Consumer.
// 매주 금요일 cron 시각에 트리거. 지난 7일 PM AgentRun output 을 workText 로 조합 후 Work Reviewer 실행.
@Processor(WEEKLY_SUMMARY_QUEUE)
export class WeeklySummaryConsumer extends WorkerHost {
  private readonly logger = new Logger(WeeklySummaryConsumer.name);

  constructor(
    private readonly generateWorklogUsecase: GenerateWorklogUsecase,
    private readonly agentRunService: AgentRunService,
    @Inject(SLACK_NOTIFIER_PORT)
    private readonly slackNotifier: SlackNotifierPort,
  ) {
    super();
  }

  async process(job: Job<WeeklySummaryJobData>): Promise<void> {
    const { ownerSlackUserId, target } = job.data;
    this.logger.log(`Weekly Summary 시작 — owner=${ownerSlackUserId} → target=${target}`);

    const runs = await this.agentRunService.findRecentSucceededRuns({
      agentType: AgentType.PM,
      slackUserId: ownerSlackUserId,
      sinceDays: 7,
      limit: 7,
    });

    if (runs.length === 0) {
      await this.slackNotifier.postMessage({
        target,
        text: '이번 주 PM AgentRun 기록이 없습니다. Weekly Summary 를 생성하지 않습니다.',
      });
      return;
    }

    const planLines = runs.map((run) => {
      const plan = coerceToDailyPlan(run.output);
      if (!plan) return null;
      const taskTitles = plan.tasks.map((t) => `- ${t.title}`).join('\n');
      return `[${run.endedAt.toISOString().slice(0, 10)}]\n${taskTitles}`;
    }).filter((line): line is string => line !== null);

    const workText = `이번 주 일일 plan 요약 (자동 생성):\n\n${planLines.join('\n\n')}`;

    const outcome = await this.generateWorklogUsecase.execute({
      workText,
      slackUserId: ownerSlackUserId,
      triggerType: TriggerType.WEEKLY_SUMMARY_CRON,
    });

    const text = formatDailyReview(outcome.result) + formatModelFooter(outcome);
    await this.slackNotifier.postMessage({ target, text });
    this.logger.log(`Weekly Summary 발송 완료 — target=${target}`);
  }
}
```

- [ ] **Step 8: GenerateWorklogUsecase 에 triggerType 파라미터 추가**

`src/agent/work-reviewer/domain/work-reviewer.type.ts` 의 `GenerateWorklogInput` 에 추가:

```typescript
import { TriggerType } from '../../../agent-run/domain/agent-run.type';

export interface GenerateWorklogInput {
  workText: string;
  slackUserId: string;
  triggerType?: TriggerType;  // optional — 미전달 시 기존 SLACK_COMMAND_WORKLOG 사용
}
```

`src/agent/work-reviewer/application/generate-worklog.usecase.ts` 에서 `agentRunService.execute` 호출 시:

```typescript
      triggerType: triggerType ?? TriggerType.SLACK_COMMAND_WORKLOG,
```

- [ ] **Step 9: WeeklySummaryModule 생성**

`src/weekly-summary/weekly-summary.module.ts` 생성:

```typescript
import { BullModule } from '@nestjs/bullmq';
import { Module } from '@nestjs/common';

import { AgentRunModule } from '../agent-run/agent-run.module';
import { WorkReviewerModule } from '../agent/work-reviewer/work-reviewer.module';
import { SlackModule } from '../slack/slack.module';
import { SlackService } from '../slack/slack.service';
import { SLACK_NOTIFIER_PORT } from '../morning-briefing/domain/port/slack-notifier.port';
import { WeeklySummaryScheduler } from './application/weekly-summary.scheduler';
import { WEEKLY_SUMMARY_QUEUE } from './domain/weekly-summary.type';
import { WeeklySummaryConsumer } from './infrastructure/weekly-summary.consumer';

@Module({
  imports: [
    BullModule.registerQueue({ name: WEEKLY_SUMMARY_QUEUE }),
    WorkReviewerModule,
    AgentRunModule,
    SlackModule,
  ],
  providers: [
    WeeklySummaryScheduler,
    WeeklySummaryConsumer,
    {
      provide: SLACK_NOTIFIER_PORT,
      useExisting: SlackService,
    },
  ],
})
export class WeeklySummaryModule {}
```

- [ ] **Step 10: AppModule 에 WeeklySummaryModule 추가**

`src/app.module.ts`:

```typescript
import { WeeklySummaryModule } from './weekly-summary/weekly-summary.module';

// imports 배열에 추가:
    WeeklySummaryModule,
```

- [ ] **Step 11: lint + test + build 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -30
```

Expected: 3중 green

---

## Task 3: OPS-3 Slack Reaction → Inbox

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `src/slack-inbox/domain/slack-inbox.type.ts`
- Create: `src/slack-inbox/domain/port/slack-inbox.repository.port.ts`
- Create: `src/slack-inbox/application/slack-inbox.service.ts`
- Create: `src/slack-inbox/application/slack-inbox.service.spec.ts`
- Create: `src/slack-inbox/infrastructure/slack-inbox.prisma.repository.ts`
- Create: `src/slack-inbox/slack-inbox.module.ts`
- Modify: `src/slack/slack.service.ts`
- Modify: `src/agent/pm/application/daily-plan-context.collector.ts`
- Modify: `src/agent/pm/pm-agent.module.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1: Prisma 스키마에 SlackInboxItem 추가**

`prisma/schema.prisma` 끝에 추가:

```prisma
// OPS-3 Slack Reaction → Inbox
// 사용자가 메시지에 configured emoji (기본 :raised_hand:) 를 달면 익일 /today 에 자동 흡수.
// consumed=true 이면 이미 plan 컨텍스트에 주입됐음. consumedAt 으로 언제 소비됐는지 추적.
model SlackInboxItem {
  id          Int       @id @default(autoincrement())
  channelId   String    @map("channel_id")
  messageTs   String    @map("message_ts")
  text        String    @db.Text
  addedAt     DateTime  @default(now()) @map("added_at")
  consumed    Boolean   @default(false)
  consumedAt  DateTime? @map("consumed_at")

  @@unique([channelId, messageTs])
  @@index([consumed, addedAt])
  @@map("slack_inbox_item")
}
```

- [ ] **Step 2: db push 실행**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm prisma db push 2>&1 | tail -10
```

Expected: `Your database is now in sync with your Prisma schema.`

- [ ] **Step 3: 도메인 타입 + 포트 정의**

`src/slack-inbox/domain/slack-inbox.type.ts` 생성:

```typescript
export interface SlackInboxItem {
  id: number;
  channelId: string;
  messageTs: string;
  text: string;
  addedAt: Date;
  consumed: boolean;
}
```

`src/slack-inbox/domain/port/slack-inbox.repository.port.ts` 생성:

```typescript
import { SlackInboxItem } from '../slack-inbox.type';

export const SLACK_INBOX_REPOSITORY_PORT = Symbol('SLACK_INBOX_REPOSITORY_PORT');

export interface SlackInboxRepositoryPort {
  upsert(item: { channelId: string; messageTs: string; text: string }): Promise<void>;
  findPending(): Promise<SlackInboxItem[]>;
  markConsumed(ids: number[]): Promise<void>;
}
```

- [ ] **Step 4: 서비스 테스트 작성 (실패 확인)**

`src/slack-inbox/application/slack-inbox.service.spec.ts` 생성:

```typescript
import { SlackInboxService } from './slack-inbox.service';

describe('SlackInboxService', () => {
  const mockRepo = {
    upsert: jest.fn(),
    findPending: jest.fn(),
    markConsumed: jest.fn(),
  };

  const service = new SlackInboxService(mockRepo as any);

  beforeEach(() => jest.clearAllMocks());

  it('addItem 은 repository.upsert 를 호출한다', async () => {
    await service.addItem({ channelId: 'C1', messageTs: '123.456', text: 'hello' });
    expect(mockRepo.upsert).toHaveBeenCalledWith({ channelId: 'C1', messageTs: '123.456', text: 'hello' });
  });

  it('consumePending 은 pending 항목을 반환하고 markConsumed 를 호출한다', async () => {
    const items = [{ id: 1, channelId: 'C1', messageTs: '1', text: 'a', addedAt: new Date(), consumed: false }];
    mockRepo.findPending.mockResolvedValue(items);
    const result = await service.consumePending();
    expect(result).toEqual(items);
    expect(mockRepo.markConsumed).toHaveBeenCalledWith([1]);
  });
});
```

- [ ] **Step 5: 테스트 실행 확인 (실패)**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="slack-inbox.service.spec" 2>&1 | tail -10
```

Expected: `Cannot find module './slack-inbox.service'`

- [ ] **Step 6: SlackInboxService 구현**

`src/slack-inbox/application/slack-inbox.service.ts` 생성:

```typescript
import { Inject, Injectable } from '@nestjs/common';

import { SlackInboxItem } from '../domain/slack-inbox.type';
import {
  SLACK_INBOX_REPOSITORY_PORT,
  SlackInboxRepositoryPort,
} from '../domain/port/slack-inbox.repository.port';

@Injectable()
export class SlackInboxService {
  constructor(
    @Inject(SLACK_INBOX_REPOSITORY_PORT)
    private readonly repository: SlackInboxRepositoryPort,
  ) {}

  async addItem(item: { channelId: string; messageTs: string; text: string }): Promise<void> {
    await this.repository.upsert(item);
  }

  // pending 항목을 가져오고 즉시 consumed 로 마킹 — 중복 주입 방지.
  async consumePending(): Promise<SlackInboxItem[]> {
    const items = await this.repository.findPending();
    if (items.length > 0) {
      await this.repository.markConsumed(items.map((i) => i.id));
    }
    return items;
  }
}
```

- [ ] **Step 7: Prisma 리포지토리 구현**

`src/slack-inbox/infrastructure/slack-inbox.prisma.repository.ts` 생성:

```typescript
import { Injectable } from '@nestjs/common';

import { PrismaService } from '../../prisma/prisma.service';
import { SlackInboxRepositoryPort } from '../domain/port/slack-inbox.repository.port';
import { SlackInboxItem } from '../domain/slack-inbox.type';

@Injectable()
export class SlackInboxPrismaRepository implements SlackInboxRepositoryPort {
  constructor(private readonly prisma: PrismaService) {}

  async upsert(item: { channelId: string; messageTs: string; text: string }): Promise<void> {
    await this.prisma.slackInboxItem.upsert({
      where: { channelId_messageTs: { channelId: item.channelId, messageTs: item.messageTs } },
      create: item,
      update: {},
    });
  }

  async findPending(): Promise<SlackInboxItem[]> {
    const rows = await this.prisma.slackInboxItem.findMany({
      where: { consumed: false },
      orderBy: { addedAt: 'asc' },
    });
    return rows.map((r) => ({
      id: r.id,
      channelId: r.channelId,
      messageTs: r.messageTs,
      text: r.text,
      addedAt: r.addedAt,
      consumed: r.consumed,
    }));
  }

  async markConsumed(ids: number[]): Promise<void> {
    await this.prisma.slackInboxItem.updateMany({
      where: { id: { in: ids } },
      data: { consumed: true, consumedAt: new Date() },
    });
  }
}
```

- [ ] **Step 8: SlackInboxModule 생성**

`src/slack-inbox/slack-inbox.module.ts` 생성:

```typescript
import { Module } from '@nestjs/common';

import { PrismaModule } from '../prisma/prisma.module';
import { SlackInboxService } from './application/slack-inbox.service';
import { SLACK_INBOX_REPOSITORY_PORT } from './domain/port/slack-inbox.repository.port';
import { SlackInboxPrismaRepository } from './infrastructure/slack-inbox.prisma.repository';

@Module({
  imports: [PrismaModule],
  providers: [
    SlackInboxService,
    {
      provide: SLACK_INBOX_REPOSITORY_PORT,
      useClass: SlackInboxPrismaRepository,
    },
  ],
  exports: [SlackInboxService],
})
export class SlackInboxModule {}
```

- [ ] **Step 9: SlackService 에 reaction_added 핸들러 등록**

`src/slack/slack.service.ts`:

1. constructor 에 `private readonly slackInboxService: SlackInboxService` 추가
2. `registerCommands(app)` 호출 이후에 추가:

```typescript
  private registerReactionHandlers(app: App): void {
    const inboxEmoji = this.configService.get<string>('SLACK_INBOX_EMOJI') ?? 'raised_hand';

    app.event('reaction_added', async ({ event, client }) => {
      if (event.reaction !== inboxEmoji) return;
      if (event.item.type !== 'message') return;

      try {
        const history = await client.conversations.history({
          channel: event.item.channel,
          latest: event.item.ts,
          inclusive: true,
          limit: 1,
        });
        const message = history.messages?.[0];
        if (!message?.text) return;

        await this.slackInboxService.addItem({
          channelId: event.item.channel,
          messageTs: event.item.ts,
          text: message.text,
        });
        this.logger.log(`Slack Inbox 추가 — channel=${event.item.channel} ts=${event.item.ts}`);
      } catch (error: unknown) {
        this.logger.warn(`Slack Inbox 추가 실패: ${error instanceof Error ? error.message : String(error)}`);
      }
    });
  }
```

`onModuleInit` 에서 `await app.start()` 직전에 `this.registerReactionHandlers(app)` 호출 추가.

- [ ] **Step 10: DailyPlanContextCollector 에 inbox 항목 주입**

`src/agent/pm/application/daily-plan-context.collector.ts`:

1. `SlackInboxService` import 추가
2. constructor 에 `private readonly slackInboxService: SlackInboxService` 추가
3. `DailyPlanContext` 인터페이스에 `inboxItems: string[]` 추가
4. `collect()` 의 `Promise.all` 에 추가:

```typescript
      this.fetchInboxItemsOrEmpty(),
```

5. 새 메서드 추가:

```typescript
  private async fetchInboxItemsOrEmpty(): Promise<string[]> {
    try {
      const items = await this.slackInboxService.consumePending();
      return items.map((i) => i.text);
    } catch (error: unknown) {
      this.logger.warn(`Slack Inbox 수집 실패: ${error instanceof Error ? error.message : String(error)}`);
      return [];
    }
  }
```

6. return 객체에 `inboxItems` 추가.

- [ ] **Step 11: PM 프롬프트 빌더에 inboxItems 섹션 반영**

`src/agent/pm/application/daily-plan-prompt.builder.ts` 에서 `DailyPlanContext.inboxItems` 가 있으면:

```typescript
if (context.inboxItems.length > 0) {
  sections.push('[Slack Inbox (✋ 반응 저장 항목)]');
  sections.push(context.inboxItems.map((t) => `- ${t}`).join('\n'));
}
```

정확한 위치와 변수명은 해당 파일의 기존 패턴을 따른다.

- [ ] **Step 12: PmAgentModule 에 SlackInboxModule import 추가**

`src/agent/pm/pm-agent.module.ts` 에 `SlackInboxModule` import 추가.

- [ ] **Step 13: AppModule 에 SlackInboxModule 추가**

`src/app.module.ts` 의 imports 배열에 `SlackInboxModule` 추가.

- [ ] **Step 14: 테스트 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="slack-inbox.service.spec" 2>&1 | tail -10
```

Expected: `Tests: 2 passed`

- [ ] **Step 15: lint + test + build 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -30
```

Expected: 3중 green

---

## Task 4: PM-3' FTS 기반 유사 plan 추출

**Files:**
- Modify: `src/prisma/prisma.service.ts`
- Modify: `src/agent-run/domain/port/agent-run.repository.port.ts`
- Modify: `src/agent-run/infrastructure/agent-run.prisma.repository.ts`
- Modify: `src/agent/pm/application/daily-plan-context.collector.ts`
- Modify: `src/agent/pm/application/daily-plan-prompt.builder.ts`

- [ ] **Step 1: PrismaService onModuleInit 에서 GIN 인덱스 생성**

`src/prisma/prisma.service.ts` 에 `OnModuleInit` 추가:

```typescript
import { Injectable, Logger, OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import { PrismaClient } from '@prisma/client';

@Injectable()
export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
  private readonly logger = new Logger(PrismaService.name);

  async onModuleInit(): Promise<void> {
    await this.$connect();
    // PM-3': agent_run.output 텍스트에 GIN 인덱스 생성 (tsvector 변환 후 FTS 쿼리 가속).
    // IF NOT EXISTS 로 멱등성 보장 — 재부팅 때마다 실행해도 안전.
    try {
      await this.$executeRaw`
        CREATE INDEX CONCURRENTLY IF NOT EXISTS idx_agent_run_output_fts
        ON agent_run USING GIN (to_tsvector('simple', COALESCE(output::text, '')))
      `;
    } catch (error: unknown) {
      // CONCURRENTLY 는 트랜잭션 밖에서만 가능 — 실패 시 warn 만 (앱 부팅 블록 금지).
      this.logger.warn(`GIN 인덱스 생성 실패 (FTS 동작은 하지만 느려질 수 있음): ${error instanceof Error ? error.message : String(error)}`);
    }
  }

  async onModuleDestroy(): Promise<void> {
    await this.$disconnect();
  }
}
```

기존에 `$connect()` 가 이미 있으면 중복 제거.

- [ ] **Step 2: SimilarPlanRow 인터페이스 + findSimilarPlans 메서드를 port 에 추가**

`src/agent-run/domain/port/agent-run.repository.port.ts`:

```typescript
export interface SimilarPlanRow {
  id: number;
  output: unknown;
  endedAt: Date;
  rank: number;
}

// AgentRunRepositoryPort 인터페이스에 추가:
  findSimilarPlans(input: {
    query: string;
    agentType: string;
    limit: number;
    excludeRunId?: number;
  }): Promise<SimilarPlanRow[]>;
```

- [ ] **Step 3: findSimilarPlans 구현 (queryRaw)**

`src/agent-run/infrastructure/agent-run.prisma.repository.ts` 에 추가:

```typescript
  async findSimilarPlans({
    query,
    agentType,
    limit,
    excludeRunId,
  }: {
    query: string;
    agentType: string;
    limit: number;
    excludeRunId?: number;
  }): Promise<SimilarPlanRow[]> {
    // to_tsquery 는 파싱 오류를 낼 수 있으므로 plainto_tsquery 사용 (공백 → AND 쿼리).
    const rows = await this.prisma.$queryRaw<
      { id: number; output: unknown; ended_at: Date; rank: number }[]
    >`
      SELECT
        id,
        output,
        ended_at,
        ts_rank(to_tsvector('simple', COALESCE(output::text, '')), plainto_tsquery('simple', ${query})) AS rank
      FROM agent_run
      WHERE
        agent_type = ${agentType}
        AND status = 'SUCCEEDED'
        AND output IS NOT NULL
        ${excludeRunId != null ? Prisma.sql`AND id != ${excludeRunId}` : Prisma.empty}
        AND to_tsvector('simple', COALESCE(output::text, '')) @@ plainto_tsquery('simple', ${query})
      ORDER BY rank DESC
      LIMIT ${limit}
    `;

    return rows.map((r) => ({
      id: r.id,
      output: r.output,
      endedAt: r.ended_at,
      rank: Number(r.rank),
    }));
  }
```

import 에 `import { Prisma } from '@prisma/client';` 추가.

- [ ] **Step 4: DailyPlanContext 에 similarPlans 필드 추가**

`src/agent/pm/application/daily-plan-context.collector.ts`:

1. `SimilarPlanRow` import 추가
2. `DailyPlanContext` 에 `similarPlans: SimilarPlanSummary[]` 추가
3. `collect()` 의 `Promise.all` 에 `this.fetchSimilarPlansOrEmpty({ userText })` 추가
4. 새 메서드 추가:

```typescript
  private async fetchSimilarPlansOrEmpty({
    userText,
  }: {
    userText: string;
  }): Promise<SimilarPlanRow[]> {
    if (userText.trim().length < 5) return [];
    try {
      return await this.agentRunService.findSimilarPlans({
        query: userText,
        agentType: AgentType.PM,
        limit: 3,
      });
    } catch (error: unknown) {
      this.logger.warn(`유사 plan FTS 실패 (컨텍스트 없이 계속): ${error instanceof Error ? error.message : String(error)}`);
      return [];
    }
  }
```

`AgentRunService` 에 `findSimilarPlans` 위임 메서드 추가 필요.

- [ ] **Step 5: AgentRunService 에 findSimilarPlans 위임 메서드 추가**

`src/agent-run/application/agent-run.service.ts`:

```typescript
  async findSimilarPlans(input: {
    query: string;
    agentType: AgentType;
    limit: number;
    excludeRunId?: number;
  }): Promise<SimilarPlanRow[]> {
    return this.repository.findSimilarPlans({
      ...input,
      agentType: input.agentType as string,
    });
  }
```

`SimilarPlanRow` import 추가.

- [ ] **Step 6: PM 프롬프트 빌더에 similarPlans 섹션 추가**

`src/agent/pm/application/daily-plan-prompt.builder.ts` 에서 `DailyPlanContext.similarPlans` 처리:

```typescript
if (context.similarPlans && context.similarPlans.length > 0) {
  sections.push('[유사 plan 참고 (FTS top 3)]');
  for (const p of context.similarPlans) {
    const plan = coerceToDailyPlan(p.output);
    if (plan) {
      const tasks = plan.tasks.slice(0, 5).map((t) => `  - ${t.title}`).join('\n');
      sections.push(`• ${p.endedAt.toISOString().slice(0, 10)} (rank=${p.rank.toFixed(3)})\n${tasks}`);
    }
  }
}
```

- [ ] **Step 7: lint + test + build 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -30
```

Expected: 3중 green

---

## Task 5: PO-1 `/po-expand`

**Files:**
- Modify: `src/model-router/domain/model-router.type.ts`
- Modify: `src/agent-run/domain/agent-run.type.ts`
- Create: `src/agent/po-expand/domain/po-expand.type.ts`
- Create: `src/agent/po-expand/domain/po-expand-error-code.enum.ts`
- Create: `src/agent/po-expand/domain/po-expand.exception.ts`
- Create: `src/agent/po-expand/domain/prompt/po-expand-system.prompt.ts`
- Create: `src/agent/po-expand/domain/prompt/po-expand.parser.ts`
- Create: `src/agent/po-expand/application/generate-po-outline.usecase.ts`
- Create: `src/agent/po-expand/application/generate-po-outline.usecase.spec.ts`
- Create: `src/agent/po-expand/infrastructure/po-expand.applier.ts`
- Create: `src/agent/po-expand/po-expand.module.ts`
- Modify: `src/slack/handler/agent-command.handler.ts`
- Modify: `src/slack/slack.service.ts`
- Modify: `src/app.module.ts`

- [ ] **Step 1: AgentType 에 PO_EXPAND 추가**

`src/model-router/domain/model-router.type.ts`:

```typescript
  PO_EXPAND = 'PO_EXPAND',
```

- [ ] **Step 2: TriggerType 에 SLACK_COMMAND_PO_EXPAND 추가**

`src/agent-run/domain/agent-run.type.ts`:

```typescript
  SLACK_COMMAND_PO_EXPAND = 'SLACK_COMMAND_PO_EXPAND',
```

- [ ] **Step 3: po-expand 도메인 정의**

`src/agent/po-expand/domain/po-expand-error-code.enum.ts` 생성:

```typescript
export enum PoExpandErrorCode {
  EMPTY_SUBJECT = 'EMPTY_SUBJECT',
}
```

`src/agent/po-expand/domain/po-expand.exception.ts` 생성:

```typescript
import { DomainStatus } from '../../../common/exception/domain-status.enum';
import { DomainException } from '../../../common/exception/domain.exception';
import { PoExpandErrorCode } from './po-expand-error-code.enum';

export class PoExpandException extends DomainException {
  constructor(params: { code: PoExpandErrorCode; message: string; status: DomainStatus }) {
    super({ ...params, domain: 'PO_EXPAND' });
  }
  get poExpandErrorCode(): PoExpandErrorCode {
    return this.code as PoExpandErrorCode;
  }
}
```

`src/agent/po-expand/domain/po-expand.type.ts` 생성:

```typescript
export interface GeneratePoOutlineInput {
  subject: string;
  slackUserId: string;
}

export interface PoOutline {
  subject: string;
  outline: string[];         // 3~5 줄 개요
  clarifyingQuestions: string[]; // Stage 1 에서 사용자에게 되묻는 질문
}
```

- [ ] **Step 4: 시스템 프롬프트 + 파서 작성**

`src/agent/po-expand/domain/prompt/po-expand-system.prompt.ts` 생성:

```typescript
export const PO_EXPAND_OUTLINE_SYSTEM_PROMPT = `당신은 프로덕트 오너입니다.
사용자가 한 줄 아이디어를 주면 3~5 줄 개요(outline)와 clarifying_questions(2~3개)를 JSON 으로 반환합니다.

응답 형식:
\`\`\`json
{
  "outline": ["항목1", "항목2", "항목3"],
  "clarifyingQuestions": ["질문1", "질문2"]
}
\`\`\`

outline 은 PRD 로 확장하기 전 검토 가능한 수준의 간결한 항목. hallucination 금지 — 모르는 도메인은 질문으로 대신한다.`;
```

`src/agent/po-expand/domain/prompt/po-expand.parser.ts` 생성:

```typescript
import { PoOutline } from '../po-expand.type';

export const parsePoOutline = (subject: string, text: string): PoOutline => {
  const match = text.match(/```json\s*([\s\S]*?)```/);
  if (!match) {
    return { subject, outline: [text.trim()], clarifyingQuestions: [] };
  }
  try {
    const parsed = JSON.parse(match[1]) as { outline?: unknown; clarifyingQuestions?: unknown };
    return {
      subject,
      outline: Array.isArray(parsed.outline) ? (parsed.outline as string[]) : [],
      clarifyingQuestions: Array.isArray(parsed.clarifyingQuestions)
        ? (parsed.clarifyingQuestions as string[])
        : [],
    };
  } catch {
    return { subject, outline: [text.trim()], clarifyingQuestions: [] };
  }
};
```

- [ ] **Step 5: GeneratePoOutlineUsecase 테스트 작성 (실패 확인)**

`src/agent/po-expand/application/generate-po-outline.usecase.spec.ts` 생성:

```typescript
import { GeneratePoOutlineUsecase } from './generate-po-outline.usecase';

describe('GeneratePoOutlineUsecase', () => {
  it('subject 가 비어 있으면 PoExpandException 발생', async () => {
    const usecase = new GeneratePoOutlineUsecase(null as any, null as any);
    await expect(usecase.execute({ subject: '  ', slackUserId: 'U1' })).rejects.toThrow();
  });
});
```

- [ ] **Step 6: 테스트 실행 확인 (실패)**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="generate-po-outline.usecase.spec" 2>&1 | tail -10
```

Expected: `Cannot find module './generate-po-outline.usecase'`

- [ ] **Step 7: GeneratePoOutlineUsecase 구현**

`src/agent/po-expand/application/generate-po-outline.usecase.ts` 생성:

```typescript
import { Injectable } from '@nestjs/common';

import { AgentRunOutcome, AgentRunService } from '../../../agent-run/application/agent-run.service';
import { TriggerType } from '../../../agent-run/domain/agent-run.type';
import { DomainStatus } from '../../../common/exception/domain-status.enum';
import { ModelRouterUsecase } from '../../../model-router/application/model-router.usecase';
import { AgentType } from '../../../model-router/domain/model-router.type';
import { PoExpandErrorCode } from '../domain/po-expand-error-code.enum';
import { PoExpandException } from '../domain/po-expand.exception';
import { parsePoOutline } from '../domain/prompt/po-expand.parser';
import { PO_EXPAND_OUTLINE_SYSTEM_PROMPT } from '../domain/prompt/po-expand-system.prompt';
import { GeneratePoOutlineInput, PoOutline } from '../domain/po-expand.type';

@Injectable()
export class GeneratePoOutlineUsecase {
  constructor(
    private readonly modelRouter: ModelRouterUsecase,
    private readonly agentRunService: AgentRunService,
  ) {}

  async execute({
    subject,
    slackUserId,
  }: GeneratePoOutlineInput): Promise<AgentRunOutcome<PoOutline>> {
    const trimmed = subject.trim();
    if (trimmed.length === 0) {
      throw new PoExpandException({
        code: PoExpandErrorCode.EMPTY_SUBJECT,
        message: '아이디어 한 줄이 비어 있습니다. `/po-expand <아이디어>` 형식으로 입력해주세요.',
        status: DomainStatus.BAD_REQUEST,
      });
    }

    return this.agentRunService.execute({
      agentType: AgentType.PO_EXPAND,
      triggerType: TriggerType.SLACK_COMMAND_PO_EXPAND,
      inputSnapshot: { subject: trimmed, slackUserId },
      evidence: [
        { sourceType: 'SLACK_COMMAND_PO_EXPAND', sourceId: slackUserId, payload: { subject: trimmed } },
      ],
      run: async () => {
        const completion = await this.modelRouter.route({
          agentType: AgentType.PO_EXPAND,
          request: { prompt: trimmed, systemPrompt: PO_EXPAND_OUTLINE_SYSTEM_PROMPT },
        });
        const outline = parsePoOutline(trimmed, completion.text);
        return { result: outline, modelUsed: completion.modelUsed, output: outline };
      },
    });
  }
}
```

- [ ] **Step 8: PoExpandApplier 구현 (Stage 2 — full PRD)**

`src/agent/po-expand/infrastructure/po-expand.applier.ts` 생성:

```typescript
import { Injectable, Logger } from '@nestjs/common';

import { ModelRouterUsecase } from '../../../model-router/application/model-router.usecase';
import { AgentType } from '../../../model-router/domain/model-router.type';
import { PreviewApplierPort } from '../../../preview-gate/domain/port/preview-applier.port';
import { PREVIEW_KIND } from '../../../preview-gate/domain/preview-action.type';

const PRD_EXPAND_SYSTEM_PROMPT = `당신은 프로덕트 오너입니다.
주어진 outline 을 바탕으로 완전한 PRD(Product Requirements Document)를 Markdown 형식으로 작성합니다.
## Goal / Background / User Stories / Acceptance Criteria / Out of Scope 섹션을 포함합니다.`;

@Injectable()
export class PoExpandApplier implements PreviewApplierPort {
  readonly kind = PREVIEW_KIND.PO_EXPAND;
  private readonly logger = new Logger(PoExpandApplier.name);

  constructor(private readonly modelRouter: ModelRouterUsecase) {}

  async apply(payload: unknown): Promise<string> {
    const { subject, outlineText } = payload as { subject: string; outlineText: string };
    this.logger.log(`PoExpand Stage 2 — subject="${subject}"`);

    const completion = await this.modelRouter.route({
      agentType: AgentType.PO_EXPAND,
      request: {
        prompt: `[아이디어]\n${subject}\n\n[Outline]\n${outlineText}`,
        systemPrompt: PRD_EXPAND_SYSTEM_PROMPT,
      },
    });
    return completion.text;
  }
}
```

- [ ] **Step 9: PoExpandModule 생성**

`src/agent/po-expand/po-expand.module.ts` 생성:

```typescript
import { Module } from '@nestjs/common';

import { AgentRunModule } from '../../agent-run/agent-run.module';
import { ModelRouterModule } from '../../model-router/model-router.module';
import { GeneratePoOutlineUsecase } from './application/generate-po-outline.usecase';
import { PoExpandApplier } from './infrastructure/po-expand.applier';

@Module({
  imports: [AgentRunModule, ModelRouterModule],
  providers: [GeneratePoOutlineUsecase, PoExpandApplier],
  exports: [GeneratePoOutlineUsecase, PoExpandApplier],
})
export class PoExpandModule {}
```

- [ ] **Step 10: /po-expand 핸들러 추가 (agent-command.handler.ts)**

`src/slack/handler/agent-command.handler.ts`:

```typescript
// deps 에 추가:
    generatePoOutlineUsecase: GeneratePoOutlineUsecase;
    createPreviewUsecase: CreatePreviewUsecase;

// handlers 끝에 추가:
  app.command('/po-expand', async ({ ack, command, respond }) => {
    const subject = command.text?.trim() ?? '';
    if (subject.length === 0) {
      await ack({ response_type: 'ephemeral', text: '사용법: `/po-expand <아이디어 한 줄>` (예: `/po-expand 결제 실패 자동 재시도`)' });
      return;
    }
    await ack({ response_type: 'ephemeral', text: '이대리(PO 모드) 가 개요를 작성 중입니다 (5~15초 소요)...' });

    await runAgentCommand({
      respond,
      logger: deps.logger,
      commandLabel: '/po-expand (Stage 1)',
      execute: () => deps.generatePoOutlineUsecase.execute({ subject, slackUserId: command.user_id }),
      format: (outline) => {
        const lines = [
          `*📋 ${outline.subject} — 개요*`,
          '',
          outline.outline.map((l) => `• ${l}`).join('\n'),
        ];
        if (outline.clarifyingQuestions.length > 0) {
          lines.push('', '*명확화 질문:*');
          outline.clarifyingQuestions.forEach((q) => lines.push(`• ${q}`));
        }
        lines.push('', '_✅ 버튼을 눌러 전체 PRD 를 생성합니다._');
        return lines.join('\n');
      },
    });
    // Note: Stage 2 (full PRD via PreviewGate) 는 향후 /po-expand-confirm <runId> 로 연결.
    // 현재는 outline 텍스트만 제공.
  });
```

- [ ] **Step 11: SlackService 에 GeneratePoOutlineUsecase 주입**

`src/slack/slack.service.ts`:
- constructor 에 `private readonly generatePoOutlineUsecase: GeneratePoOutlineUsecase` 추가
- `registerAgentCommandHandlers` 호출에 `generatePoOutlineUsecase: this.generatePoOutlineUsecase` 추가
- import 추가

- [ ] **Step 12: AppModule 에 PoExpandModule 추가 + PreviewGate forRoot 에 applier 등록**

`src/app.module.ts`:

```typescript
import { PoExpandModule } from './agent/po-expand/po-expand.module';
import { PoExpandApplier } from './agent/po-expand/infrastructure/po-expand.applier';

// imports 에 PoExpandModule 추가
// PreviewGateModule.forRoot 의 appliers 에 PoExpandApplier 추가:
    PreviewGateModule.forRoot({
      appliers: [PmWriteBackApplier, PoExpandApplier],
      imports: [GithubModule, NotionModule, ModelRouterModule],
    }),
```

- [ ] **Step 13: 테스트 통과 + lint + build 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="generate-po-outline.usecase.spec" 2>&1 | tail -10
```

Expected: `Tests: 1 passed`

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -30
```

Expected: 3중 green

---

## Task 6: QA-1 Reviewer 룰셋 학습

**Files:**
- Modify: `prisma/schema.prisma`
- Create: `src/agent/code-reviewer/domain/pr-review-outcome.type.ts`
- Create: `src/agent/code-reviewer/domain/port/pr-review-outcome.repository.port.ts`
- Create: `src/agent/code-reviewer/application/save-review-outcome.usecase.ts`
- Create: `src/agent/code-reviewer/application/save-review-outcome.usecase.spec.ts`
- Create: `src/agent/code-reviewer/infrastructure/pr-review-outcome.prisma.repository.ts`
- Modify: `src/agent/code-reviewer/application/review-pull-request.usecase.ts`
- Modify: `src/agent/code-reviewer/code-reviewer.module.ts`
- Modify: `src/agent-run/domain/agent-run.type.ts`
- Modify: `src/slack/handler/agent-command.handler.ts`
- Modify: `src/slack/slack.service.ts`

- [ ] **Step 1: Prisma 스키마에 PrReviewOutcome 추가**

`prisma/schema.prisma` 끝에 추가:

```prisma
// QA-1 Reviewer 룰셋 학습 — 사용자가 accept/reject 한 리뷰 결과를 누적해 다음 /review-pr 프롬프트에 negative example 주입.
model PrReviewOutcome {
  id          Int      @id @default(autoincrement())
  agentRunId  Int      @map("agent_run_id")
  slackUserId String   @map("slack_user_id")
  accepted    Boolean
  comment     String?  @db.Text
  createdAt   DateTime @default(now()) @map("created_at")

  @@index([slackUserId, accepted, createdAt])
  @@map("pr_review_outcome")
}
```

- [ ] **Step 2: db push 실행**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm prisma db push 2>&1 | tail -10
```

Expected: `Your database is now in sync with your Prisma schema.`

- [ ] **Step 3: 도메인 타입 + 포트 정의**

`src/agent/code-reviewer/domain/pr-review-outcome.type.ts` 생성:

```typescript
export interface PrReviewOutcome {
  id: number;
  agentRunId: number;
  slackUserId: string;
  accepted: boolean;
  comment: string | null;
  createdAt: Date;
}

export interface SaveReviewOutcomeInput {
  agentRunId: number;
  slackUserId: string;
  accepted: boolean;
  comment?: string;
}
```

`src/agent/code-reviewer/domain/port/pr-review-outcome.repository.port.ts` 생성:

```typescript
import { PrReviewOutcome, SaveReviewOutcomeInput } from '../pr-review-outcome.type';

export const PR_REVIEW_OUTCOME_REPOSITORY_PORT = Symbol('PR_REVIEW_OUTCOME_REPOSITORY_PORT');

export interface PrReviewOutcomeRepositoryPort {
  save(input: SaveReviewOutcomeInput): Promise<void>;
  // 최근 N개의 rejected 결과 — 다음 리뷰 프롬프트의 negative example 로.
  findRecentRejected(input: { slackUserId: string; limit: number }): Promise<PrReviewOutcome[]>;
}
```

- [ ] **Step 4: SaveReviewOutcomeUsecase 테스트 작성 (실패 확인)**

`src/agent/code-reviewer/application/save-review-outcome.usecase.spec.ts` 생성:

```typescript
import { SaveReviewOutcomeUsecase } from './save-review-outcome.usecase';

describe('SaveReviewOutcomeUsecase', () => {
  const mockRepo = { save: jest.fn(), findRecentRejected: jest.fn() };
  const usecase = new SaveReviewOutcomeUsecase(mockRepo as any);

  beforeEach(() => jest.clearAllMocks());

  it('save 를 repository 에 위임한다', async () => {
    await usecase.execute({ agentRunId: 1, slackUserId: 'U1', accepted: true });
    expect(mockRepo.save).toHaveBeenCalledWith({ agentRunId: 1, slackUserId: 'U1', accepted: true });
  });
});
```

- [ ] **Step 5: 테스트 실행 확인 (실패)**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="save-review-outcome.usecase.spec" 2>&1 | tail -10
```

Expected: `Cannot find module './save-review-outcome.usecase'`

- [ ] **Step 6: SaveReviewOutcomeUsecase 구현**

`src/agent/code-reviewer/application/save-review-outcome.usecase.ts` 생성:

```typescript
import { Inject, Injectable } from '@nestjs/common';

import { SaveReviewOutcomeInput } from '../domain/pr-review-outcome.type';
import {
  PR_REVIEW_OUTCOME_REPOSITORY_PORT,
  PrReviewOutcomeRepositoryPort,
} from '../domain/port/pr-review-outcome.repository.port';

@Injectable()
export class SaveReviewOutcomeUsecase {
  constructor(
    @Inject(PR_REVIEW_OUTCOME_REPOSITORY_PORT)
    private readonly repository: PrReviewOutcomeRepositoryPort,
  ) {}

  async execute(input: SaveReviewOutcomeInput): Promise<void> {
    await this.repository.save(input);
  }
}
```

- [ ] **Step 7: PrReviewOutcomePrismaRepository 구현**

`src/agent/code-reviewer/infrastructure/pr-review-outcome.prisma.repository.ts` 생성:

```typescript
import { Injectable } from '@nestjs/common';

import { PrismaService } from '../../../prisma/prisma.service';
import { PrReviewOutcomeRepositoryPort } from '../domain/port/pr-review-outcome.repository.port';
import { PrReviewOutcome, SaveReviewOutcomeInput } from '../domain/pr-review-outcome.type';

@Injectable()
export class PrReviewOutcomePrismaRepository implements PrReviewOutcomeRepositoryPort {
  constructor(private readonly prisma: PrismaService) {}

  async save(input: SaveReviewOutcomeInput): Promise<void> {
    await this.prisma.prReviewOutcome.create({ data: input });
  }

  async findRecentRejected({
    slackUserId,
    limit,
  }: {
    slackUserId: string;
    limit: number;
  }): Promise<PrReviewOutcome[]> {
    const rows = await this.prisma.prReviewOutcome.findMany({
      where: { slackUserId, accepted: false },
      orderBy: { createdAt: 'desc' },
      take: limit,
    });
    return rows.map((r) => ({
      id: r.id,
      agentRunId: r.agentRunId,
      slackUserId: r.slackUserId,
      accepted: r.accepted,
      comment: r.comment,
      createdAt: r.createdAt,
    }));
  }
}
```

- [ ] **Step 8: ReviewPullRequestUsecase 에 rejected history 주입**

`src/agent/code-reviewer/application/review-pull-request.usecase.ts`:

1. constructor 에 추가:
```typescript
    @Inject(PR_REVIEW_OUTCOME_REPOSITORY_PORT)
    private readonly outcomeRepository: PrReviewOutcomeRepositoryPort,
```

2. `run` 콜백 안에서 프롬프트 빌드 전에 추가:
```typescript
        const recentRejected = await this.outcomeRepository.findRecentRejected({
          slackUserId,
          limit: 2,
        });
        const negativeExamples = recentRejected.length > 0
          ? '\n\n[이 사용자가 이전에 무시한 리뷰 패턴 — 이런 코멘트는 피하세요]\n' +
            recentRejected.map((r) => `• ${r.comment ?? '(코멘트 없음)'}`).join('\n')
          : '';

        const prompt = buildReviewPrompt({ detail, diff }) + negativeExamples;
```

- [ ] **Step 9: CodeReviewerModule 에 repository + usecase 추가**

`src/agent/code-reviewer/code-reviewer.module.ts`:

```typescript
import { PR_REVIEW_OUTCOME_REPOSITORY_PORT } from './domain/port/pr-review-outcome.repository.port';
import { SaveReviewOutcomeUsecase } from './application/save-review-outcome.usecase';
import { PrReviewOutcomePrismaRepository } from './infrastructure/pr-review-outcome.prisma.repository';

// providers 에 추가:
    SaveReviewOutcomeUsecase,
    { provide: PR_REVIEW_OUTCOME_REPOSITORY_PORT, useClass: PrReviewOutcomePrismaRepository },
// exports 에 추가:
    SaveReviewOutcomeUsecase,
```

- [ ] **Step 10: /review-feedback 핸들러 추가 (agent-command.handler.ts)**

```typescript
// deps 에 추가:
    saveReviewOutcomeUsecase: SaveReviewOutcomeUsecase;

// handlers 끝에 추가:
  app.command('/review-feedback', async ({ ack, command, respond }) => {
    // 형식: /review-feedback <runId> accept|reject [reason]
    const parts = (command.text ?? '').trim().split(/\s+/);
    const runId = parseInt(parts[0], 10);
    const verdict = parts[1]?.toLowerCase();

    if (!parts[0] || isNaN(runId) || !['accept', 'reject'].includes(verdict)) {
      await ack({
        response_type: 'ephemeral',
        text: '사용법: `/review-feedback <AgentRun ID> accept|reject [이유]`\n예: `/review-feedback 42 reject 너무 사소한 스타일 지적`',
      });
      return;
    }
    await ack({ response_type: 'ephemeral', text: '피드백 저장 중...' });

    const accepted = verdict === 'accept';
    const comment = parts.slice(2).join(' ') || undefined;
    await deps.saveReviewOutcomeUsecase.execute({
      agentRunId: runId,
      slackUserId: command.user_id,
      accepted,
      comment,
    });
    await respond({
      response_type: 'ephemeral',
      replace_original: true,
      text: `AgentRun #${runId} 피드백 저장 완료 (${accepted ? '✅ accept' : '❌ reject'}${comment ? ` — ${comment}` : ''})`,
    });
  });
```

- [ ] **Step 11: SlackService 에 SaveReviewOutcomeUsecase 주입**

`src/slack/slack.service.ts`:
- constructor 에 추가
- `registerAgentCommandHandlers` 호출에 `saveReviewOutcomeUsecase: this.saveReviewOutcomeUsecase` 추가

- [ ] **Step 12: 테스트 통과 + 3중 green 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="save-review-outcome.usecase.spec" 2>&1 | tail -10
```

Expected: `Tests: 1 passed`

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -30
```

Expected: 3중 green

---

## Task 7: OPS-2 Webhook 수신부

**Files:**
- Create: `src/webhook/domain/webhook.type.ts`
- Create: `src/webhook/interface/webhook.controller.ts`
- Create: `src/webhook/interface/webhook.controller.spec.ts`
- Create: `src/webhook/webhook.module.ts`
- Modify: `src/main.ts`
- Modify: `src/app.module.ts`
- Modify: `src/agent-run/domain/agent-run.type.ts`

- [ ] **Step 1: TriggerType 에 WEBHOOK 추가**

`src/agent-run/domain/agent-run.type.ts`:

```typescript
  WEBHOOK = 'WEBHOOK',
```

- [ ] **Step 2: Webhook 도메인 타입 정의**

`src/webhook/domain/webhook.type.ts` 생성:

```typescript
// GitHub webhook event 유형 중 이대리가 처리하는 것만 선언.
export type WebhookEventType = 'issues.opened' | 'pull_request.opened';

export interface WebhookTriggerPayload {
  event: WebhookEventType;
  // repo 는 "owner/repo" 형식.
  repo: string;
  // 이벤트 원문 data (issue body / PR title 등).
  data: {
    number?: number;
    title?: string;
    body?: string;
    url?: string;
  };
  // 트리거 대상 slack user id — 봇이 어느 사용자 컨텍스트로 처리할지.
  slackUserId: string;
}

export const WEBHOOK_SECRET_ENV = 'WEBHOOK_SECRET';
```

- [ ] **Step 3: WebhookController 테스트 작성 (실패 확인)**

`src/webhook/interface/webhook.controller.spec.ts` 생성:

```typescript
import { Test } from '@nestjs/testing';
import { WebhookController } from './webhook.controller';
import { GenerateImpactReportUsecase } from '../../agent/impact-reporter/application/generate-impact-report.usecase';
import { ConfigService } from '@nestjs/config';
import * as crypto from 'crypto';

describe('WebhookController', () => {
  let controller: WebhookController;
  const mockUsecase = { execute: jest.fn() };
  const secret = 'test-secret';

  beforeEach(async () => {
    const module = await Test.createTestingModule({
      controllers: [WebhookController],
      providers: [
        { provide: GenerateImpactReportUsecase, useValue: mockUsecase },
        { provide: ConfigService, useValue: { get: () => secret } },
      ],
    }).compile();
    controller = module.get(WebhookController);
  });

  const makeSignature = (body: string) => {
    const hmac = crypto.createHmac('sha256', secret);
    hmac.update(body);
    return `sha256=${hmac.digest('hex')}`;
  };

  it('유효한 시그니처 + issues.opened → impact report 트리거', async () => {
    mockUsecase.execute.mockResolvedValue({ result: {}, modelUsed: 'test', agentRunId: 1 });
    const body = JSON.stringify({ event: 'issues.opened', repo: 'foo/bar', data: { number: 1, title: 'bug', body: 'desc', url: 'http://x' }, slackUserId: 'U1' });
    const sig = makeSignature(body);
    const result = await controller.trigger(body, sig);
    expect(result).toEqual({ accepted: true });
    expect(mockUsecase.execute).toHaveBeenCalled();
  });

  it('잘못된 시그니처 → 401', async () => {
    const body = JSON.stringify({ event: 'issues.opened', repo: 'foo/bar', data: {}, slackUserId: 'U1' });
    await expect(controller.trigger(body, 'sha256=badhash')).rejects.toThrow();
  });
});
```

- [ ] **Step 4: 테스트 실행 확인 (실패)**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="webhook.controller.spec" 2>&1 | tail -10
```

Expected: `Cannot find module './webhook.controller'`

- [ ] **Step 5: WebhookController 구현**

`src/webhook/interface/webhook.controller.ts` 생성:

```typescript
import {
  Body,
  Controller,
  Headers,
  HttpCode,
  HttpStatus,
  Logger,
  Post,
  UnauthorizedException,
} from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import * as crypto from 'crypto';

import { GenerateImpactReportUsecase } from '../../agent/impact-reporter/application/generate-impact-report.usecase';
import { WEBHOOK_SECRET_ENV, WebhookTriggerPayload } from '../domain/webhook.type';

// OPS-2 단일 Webhook 수신부.
// GitHub webhook (issue.opened, pr.opened) → /impact-report 자동 트리거.
// HMAC-SHA256 시그니처 검증 (X-Webhook-Signature 헤더, 형식: "sha256=<hex>").
// WEBHOOK_SECRET 미설정 시 모든 요청 거부 (운영 안전 원칙).
@Controller('v1/agent')
export class WebhookController {
  private readonly logger = new Logger(WebhookController.name);

  constructor(
    private readonly generateImpactReportUsecase: GenerateImpactReportUsecase,
    private readonly configService: ConfigService,
  ) {}

  @Post('trigger')
  @HttpCode(HttpStatus.OK)
  async trigger(
    @Body() rawBody: string,
    @Headers('x-webhook-signature') signature: string,
  ): Promise<{ accepted: boolean }> {
    this.verifySignature(rawBody, signature);

    let payload: WebhookTriggerPayload;
    try {
      payload = JSON.parse(rawBody) as WebhookTriggerPayload;
    } catch {
      throw new UnauthorizedException('Invalid JSON payload');
    }

    this.logger.log(`Webhook 수신 — event=${payload.event} repo=${payload.repo}`);

    if (payload.event === 'issues.opened' || payload.event === 'pull_request.opened') {
      const subject = `${payload.event.replace('.', ' ')} — ${payload.repo} #${payload.data.number ?? ''}: ${payload.data.title ?? ''}`;
      // non-blocking fire-and-forget (webhook response 는 빠르게)
      void this.generateImpactReportUsecase
        .execute({ subject, slackUserId: payload.slackUserId })
        .catch((err: unknown) => {
          this.logger.error(
            `Webhook impact report 실패: ${err instanceof Error ? err.message : String(err)}`,
          );
        });
    }

    return { accepted: true };
  }

  private verifySignature(rawBody: string, signature: string): void {
    const secret = this.configService.get<string>(WEBHOOK_SECRET_ENV);
    if (!secret) {
      throw new UnauthorizedException('WEBHOOK_SECRET 미설정 — 모든 webhook 요청을 거부합니다.');
    }
    if (!signature?.startsWith('sha256=')) {
      throw new UnauthorizedException('X-Webhook-Signature 헤더가 없거나 형식이 잘못됐습니다.');
    }
    const hmac = crypto.createHmac('sha256', secret);
    hmac.update(rawBody);
    const expected = `sha256=${hmac.digest('hex')}`;
    // timing-safe compare — length 다르면 0으로 패딩해 직접 비교
    const a = Buffer.from(expected);
    const b = Buffer.from(signature);
    const len = Math.max(a.length, b.length);
    const pa = Buffer.alloc(len, 0);
    const pb = Buffer.alloc(len, 0);
    a.copy(pa);
    b.copy(pb);
    if (!crypto.timingSafeEqual(pa, pb)) {
      throw new UnauthorizedException('HMAC 시그니처 불일치.');
    }
  }
}
```

- [ ] **Step 6: WebhookModule 생성**

`src/webhook/webhook.module.ts` 생성:

```typescript
import { Module } from '@nestjs/common';

import { ImpactReporterModule } from '../agent/impact-reporter/impact-reporter.module';
import { WebhookController } from './interface/webhook.controller';

@Module({
  imports: [ImpactReporterModule],
  controllers: [WebhookController],
})
export class WebhookModule {}
```

- [ ] **Step 7: main.ts 에서 raw body 파서 활성화**

`src/main.ts` 에서 NestJS HTTP 앱이 raw body 를 받도록 설정:

```typescript
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule, {
    rawBody: true,  // NestJS 10+ — req.rawBody 활성화
  });
  // WebhookController 는 @Body() string 으로 raw body 수신.
  // express body-parser text/plain 활성화.
  app.use('/v1/agent/trigger', (req: { headers: Record<string, string>; body: unknown }, _res: unknown, next: () => void) => {
    req.headers['content-type'] = 'text/plain';
    next();
  });
  await app.listen(process.env.PORT ?? 3000);
}
void bootstrap();
```

실제 main.ts 의 기존 내용을 먼저 읽고 기존 패턴을 유지하면서 rawBody: true 옵션과 content-type 미들웨어만 추가한다.

- [ ] **Step 8: AppModule 에 WebhookModule 추가**

`src/app.module.ts`:

```typescript
import { WebhookModule } from './webhook/webhook.module';

// imports 배열에 추가:
    WebhookModule,
```

- [ ] **Step 9: 테스트 통과 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm test -- --testPathPattern="webhook.controller.spec" 2>&1 | tail -10
```

Expected: `Tests: 2 passed`

- [ ] **Step 10: 최종 lint + test + build 3중 green 확인**

```bash
cd /Users/juneseok/Desktop/backend/기타/personal_agents && pnpm lint:check && pnpm test && pnpm build 2>&1 | tail -40
```

Expected: 3중 green

---

## 자기 검토 체크리스트

- [x] **Spec 커버리지**: OPS-5 / PRO-4 / OPS-3 / PM-3' / PO-1 / QA-1 / OPS-2 모두 태스크 보유
- [x] **Placeholder 없음**: 모든 스텝에 실제 코드/명령어 존재
- [x] **타입 일관성**: `SlackInboxItem`, `PrReviewOutcome`, `FailedRunSnapshot`, `WebhookTriggerPayload`, `PoOutline`, `SimilarPlanRow`, `WeeklySummaryJobData` — 정의된 파일에서 사용
- [x] **prisma db push**: schema 변경 스텝마다 명령 포함
- [x] **3중 green**: 각 태스크 끝마다 `pnpm lint:check && pnpm test && pnpm build` 스텝 보유
- [x] **commit 없음**: 사용자가 명시 요청 전까지 commit 안 함

## 주의 사항

- **PM-3' GIN 인덱스**: `CONCURRENTLY` 는 트랜잭션 밖에서만 가능. `onModuleInit` 에서 실패해도 warn 만 내고 앱 부팅 계속.
- **OPS-2 rawBody**: `@Body()` 로 string 수신 시 `content-type: text/plain` 이 필요. 기존 JSON API 영향 없도록 `/v1/agent/trigger` 경로에만 미들웨어 적용.
- **PRO-4 workText 길이 검증**: `GenerateWorklogUsecase` 에 `EMPTY_WORK_INPUT` 가드가 있으므로 Consumer 에서 runs 가 0개이면 early return 해야 함 (이미 포함됨).
- **PO-1 PreviewGate forRoot**: `PoExpandApplier` 가 `ModelRouterUsecase` 에 의존하므로 `imports` 에 `ModelRouterModule` 추가 필요.
